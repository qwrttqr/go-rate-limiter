services:
  redis:
    image: redis:latest
    container_name: shop-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REDIS_PORT=6379
    depends_on:
      - redis

volumes:
  redis_data: